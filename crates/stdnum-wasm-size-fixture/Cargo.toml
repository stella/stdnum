[package]
name = "stella-stdnum-wasm-size-fixture"
version = "0.0.0"
edition = "2024"
rust-version = "1.96"
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib"]
doctest = false

[dependencies]
stella-stdnum-core = { path = "../stdnum-core" }
wasm-bindgen = "=0.2.126"

# Keep this fixture independent so its release build uses Cargo's downstream
# defaults rather than the parent workspace's optimized release profile.
[workspace]
