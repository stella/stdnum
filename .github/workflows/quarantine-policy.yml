name: Package quarantine policy

on:
  pull_request:
  merge_group:

permissions:
  contents: read

jobs:
  enforce:
    name: Enforce package quarantine
    if: github.repository == 'stella/stdnum'
    permissions:
      contents: read
    uses: stella/.github/.github/workflows/quarantine-policy.yml@0f814e1a0c6c7401778e661209553b6e15f8d92a
