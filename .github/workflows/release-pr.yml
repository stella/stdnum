name: Release PR

on:
  push:
    branches: [main]

concurrency:
  group: release-pr-${{ github.ref }}
  cancel-in-progress: true

permissions: {}

jobs:
  version:
    name: Maintain version packages PR
    permissions:
      contents: read
    uses: stella/.github/.github/workflows/changeset-release-pr.yml@0f814e1a0c6c7401778e661209553b6e15f8d92a
    with:
      bun-version-file: package.json
      sync-cargo-inherited-lock: true
    secrets:
      CHANGELOG_APP_ID: ${{ secrets.CHANGELOG_APP_ID }}
      CHANGELOG_APP_PRIVATE_KEY: ${{ secrets.CHANGELOG_APP_PRIVATE_KEY }}
